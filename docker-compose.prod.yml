services:
  db:
    image: postgres:16-alpine
    container_name: nucleus-db
    restart: always
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-nucleus_user}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-nucleus_password}
      POSTGRES_DB: ${POSTGRES_DB:-nucleus_db}
    volumes:
      - postgres_prod_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-nucleus_user} -d ${POSTGRES_DB:-nucleus_db}"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: nucleus-redis
    restart: always
    volumes:
      - redis_prod_data:/data

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: nucleus-backend
    restart: always
    environment:
      - DATABASE_URL=postgresql+asyncpg://${POSTGRES_USER:-nucleus_user}:${POSTGRES_PASSWORD:-nucleus_password}@db:5432/${POSTGRES_DB:-nucleus_db}
      - REDIS_URL=redis://redis:6379/0
      - SECRET_KEY=${SECRET_KEY:-super-secret-production-key}
      - ENVIRONMENT=production
      - DEBUG=False
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_started

  caddy:
    image: caddy:2-alpine
    container_name: nucleus-caddy
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      - backend

volumes:
  postgres_prod_data:
  redis_prod_data:
  caddy_data:
  caddy_config:
